# Changelog

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-08-09 | 1.0.0 | Initial creation | Migration Team |
| 2026-08-10 | 1.1.0 | Rewrote `01-database-schema.md` around the NeDB → SQLite migration: append-only datafile reader, phased idempotent import, `migration_state` tracking, and the eight production tables (settings, profiles, playlists, history, watch_stats, search_history, subscription_cache, tab_sessions) with indexes, foreign keys, triggers and sqlx pool/offline setup | Migration Team |
| 2026-08-10 | 1.1.0 | Rewrote `02-tauri-commands.md` to the actual module layout: `db/settings`, `db/profiles`, `db/playlists`, `db/history`, `db/watch_stats`, `db/search_history`, `db/subscription_cache`, `db/tab_sessions`, plus `sync`, `network`, `window` and `shortcuts`; added the error model, camelCase serialisation contract and capability notes | Migration Team |
| 2026-08-10 | 1.2.0 | Added `03-yt-dlp-sidecar.md` — per-platform sidecar bundling, hardened base argv, the six sidecar commands, throttled progress events, download record persistence with crash recovery, `DENIED_CUSTOM_ARGS`/`ALLOWED_CUSTOM_ARGS` validation, and format/codec selection | Migration Team |
| 2026-08-10 | 1.2.0 | Added `04-potoken-generation.md` — hidden off-screen `WebviewWindow` generator, isolated `potoken://` protocol, `botGuardScript.js` contract, `generate_po_token` with coalescing and caching, per-attempt session cleanup, and proxy propagation | Migration Team |
| 2026-08-10 | 1.2.0 | Added `05-sync-encryption.md` — aes-gcm/x25519-dalek/hkdf/pbkdf2 stack, Argon2id root derivation with per-collection HKDF subkeys, X25519 device pairing, AAD-bound snapshot envelopes, the seven synced collections, v1 legacy decryption path, and standard/enhanced privacy modes | Migration Team |
| 2026-08-10 | 1.2.0 | Added `06-network-proxy.md` — reqwest client registry, per-request proxy resolution with playback-consistency invalidation, the `net_fetch` renderer wrapper with target-specific headers and host allow-lists, the `slyimg://` Rust image cache (chosen over ServiceWorker), and CORS/CSP handling | Migration Team |
| 2026-08-10 | 1.2.0 | Expanded `OVERVIEW.md` with the backend module map, annotated file index, suggested reading order, and an explicit note that all six documents are pre-implementation design specifications | Migration Team |
| 2026-08-12 | 1.3.0 | Updated `01-database-schema.md` to add the `download_records` table as implemented in migration 001 | Docs Update |
| 2026-08-12 | 1.3.0 | Updated `02-tauri-commands.md` — documented the serde `camelCase` contract status across all models; revised the history section to match the actual `HistoryEntry` struct (ISO string `time_watched`, nullable `watch_progress`/`length_seconds`, `is_watched` column); added frontend interface example | Docs Update |
| 2026-08-12 | 1.3.0 | Updated `03-yt-dlp-sidecar.md` — renamed commands from `ytdlp_*` to `yt_dlp_*`; documented `YtDlpState` with `tokio::sync::Mutex`; corrected `yt_dlp_list` return type to `Vec<DownloadStatus>`; updated event names to `yt-dlp-*` prefix; updated table schema to `download_records`; noted crash recovery and binary download as follow-ups | Docs Update |
| 2026-08-13 | 1.4.0 | Added `extractor/` module to backend architecture; extractor shares hidden webview pattern with PoToken — `PendingExtractions` for request-ID correlation, `extract()`/`extraction_result()`/`extractor_ready()` commands; ported youtubei.js parsers from OpenTubeX (parseListItem, parseLockupView, parseChannelHomeTab); frontend api/index.ts reordered to extractor → Invidious fallback | Migration Team |
| 2026-08-13 | 1.5.0 | Added `07-extractor-bridge.md` documenting the hidden webview youtubei.js bridge, method reference, parser inventory, and Invidious fallback chain; updated OVERVIEW file index and status | Migration Team |
| 2026-08-13 | 2.0.0 | **Phase 2 complete**: Rewrote sync module from prototype to OpenTubeX-compatible protocol. crypto.rs: PBKDF2-SHA256 (600k), AES-GCM, gzip, 64KiB padding. client.rs: /health, /account/{register,login,delete}, /v1/encrypted_sync with 409-retry. merge.rs: deletion-aware set merge + data-loss guard. commands.rs: 12 Tauri commands. Pinia sync store wired. 29 tests passing. | Migration Team |
| 2026-08-13 | 3.0.0 | **Phase 3 complete**: Domain-based header hardening in http_client.rs. URL classification (InnerTube/ServiceWorker/Image/Videoplayback/IpWhoIs/Invidious), header injection per domain, CONSENT/SOCS cookies, tracking cookie detection, response sanitization, stable desktop UA. Applied to youtube.rs, invidious.rs, sync/client.rs. 41 new tests. | Migration Team |
| 2026-08-13 | 4.0.0 | **Phase 4 complete**: SponsorBlock (segments/labels/submit/vote), DeArrow (titles/thumbnails), Return YouTube Dislike. 7 Tauri commands. 40 tests. | Migration Team |
| 2026-08-13 | 5.0.0 | **Phase 5+8 complete**: Image caching (LRU + imgcache:// protocol), datastore CRUD (history/playlist/subscription/sync-state/watch-stats repositories). 10 new DB commands. 23 image + 29 datastore tests. | Migration Team |
| 2026-08-13 | 5.1.0 | Fixed LockupView VIDEO/SHORT parsing in extractor main.ts — duration now read from ThumbnailBottomOverlayView badge, view count from regex search across metadata parts, author from structural detection. Matches OpenTubeX parseLockupView approach. | Migration Team |